import PySTSIntegrator

class STS(PySTSIntegrator.PySTSIntegrator):
   """
   Parent class for all single time-stepping propagators.
   """
   def init(self, phys, forces, prop):
      """
      Finalize the propagator.
      """
      return
   def run(self, phys, forces, prop):
      """
      Finalize the propagator.
      """
      return
   def finish(self, phys, forces, prop):
      """
      Finalize the propagator.
      """
      return
